'use client';
import Image from 'next/image'; import Link from 'next/link'; import {Menu,X,ArrowUpRight} from 'lucide-react'; import {useEffect,useState} from 'react';
export default function Navbar(){const [scrolled,setScrolled]=useState(false),[open,setOpen]=useState(false);useEffect(()=>{const f=()=>setScrolled(scrollY>20);addEventListener('scroll',f);f();return()=>removeEventListener('scroll',f)},[]);return <header className={`nav ${scrolled?'scrolled':''} ${open?'mobile-open':''}`}><div className="nav-inner"><Link href="/" className="brand" onClick={()=>setOpen(false)}><Image src="/og-logo.png" alt="OG Studios.Tech" width={310} height={124}/></Link><nav id="primary-navigation" className="nav-links" aria-label="Primary"><Link href="/#services" onClick={()=>setOpen(false)}>Services</Link><Link href="/#work" onClick={()=>setOpen(false)}>Work</Link><Link href="/#labs" onClick={()=>setOpen(false)}>Labs</Link><Link href="/classes" onClick={()=>setOpen(false)}>Classes</Link><Link href="/#about" onClick={()=>setOpen(false)}>About</Link></nav><Link href="/start-project" className="btn btn-primary nav-cta">Start a Project <ArrowUpRight size={15}/></Link><button className="menu-btn" aria-label={open?'Close menu':'Open menu'} aria-expanded={open} aria-controls="primary-navigation" onClick={()=>setOpen(v=>!v)}>{open?<X/>:<Menu/>}</button></div></header>}
