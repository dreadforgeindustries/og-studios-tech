'use client'; import {useEffect,useState} from 'react';
export default function Ecosystem(){const [p,setP]=useState({x:0,y:0});useEffect(()=>{const f=(e:MouseEvent)=>setP({x:(e.clientX/innerWidth-.5)*12,y:(e.clientY/innerHeight-.5)*12});addEventListener('mousemove',f);return()=>removeEventListener('mousemove',f)},[]);return <div className="ecosystem" style={{transform:`perspective(900px) rotateX(${-p.y*.08}deg) rotateY(${p.x*.08}deg)`}}><div className="core"><div><div className="eyebrow" style={{textAlign:'center'}}>OG</div><strong style={{fontSize:19}}>STUDIO</strong></div></div>{['Web','AI','Cloud','Database','IoT','Mobile'].map((n,i)=><div key={n} className={`node n${i+1}`}>{n}</div>)}{[1,2,3,4,5,6].map(n=><span key={n} className={`line l${n}`}/>)}</div>}
