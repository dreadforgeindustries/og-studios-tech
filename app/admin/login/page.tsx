'use client'; import {useState} from 'react'; import {useRouter} from 'next/navigation'; import {Loader2,LockKeyhole} from 'lucide-react';
export default function Login(){const [email,setEmail]=useState(''),[password,setPassword]=useState(''),[err,setErr]=useState(''),[busy,setBusy]=useState(false);const router=useRouter();const submit=async(e:React.FormEvent)=>{e.preventDefault();setBusy(true);setErr('');try{const r=await fetch('/api/admin/auth',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email,password})});const j=await r.json();if(!r.ok)throw new Error(j.error);router.push('/admin');router.refresh()}catch(e:any){setErr(e.message)}finally{setBusy(false)}};return <section className="form-shell" style={{maxWidth:520}}><div className="form-card"><div className="cap-icon"><LockKeyhole size={20}/></div><div className="eyebrow" style={{marginTop:20}}>ADMIN ACCESS</div><h1 style={{fontSize:58}}>Sign in.</h1><p className="muted">Secure dashboard access is handled by Supabase Auth. No admin password is stored in source code.</p><form onSubmit={submit}><div className="field"><label>Email</label><input type="email" required value={email} onChange={e=>setEmail(e.target.value)}/></div><div className="field" style={{marginTop:14}}><label>Password</label><input type="password" required value={password} onChange={e=>setPassword(e.target.value)}/></div>{err&&<div className="error" style={{marginTop:14}}>{err}</div>}<button className="btn btn-blue" style={{marginTop:20,width:'100%'}} disabled={busy}>{busy?<Loader2 className="spin" size={16}/>:null} Sign in</button></form></div></section>}
